module RandomGuess
  () where


-- n = 12

-- main = do
--   gen <- getStdGen
--   askForNum gen

-- askForNum :: StdGen -> IO ()
-- askForNum gen = do
--   let (randomNum, newGen)= randomR (1,10) gen :: (Int, StdGen)
--   putStrLn "which number of 1-10?:"
--   numberStr <- getLine
--   when (not null numberString) $ do
--     num = read numberStr




